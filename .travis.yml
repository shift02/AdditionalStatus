language: java
jdk: oraclejdk7
env:
  matrix:
    - GRADLE_OPTS="-Xms2048m -Xms256m -XX:MaxPermSize=256m -XX:PermSize=256m"
install:
- ./gradlew assemble
script:
- ./gradlew check

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/